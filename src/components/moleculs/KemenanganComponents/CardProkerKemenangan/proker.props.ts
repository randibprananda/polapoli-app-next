import { ProkerInterface } from '../../../../@types/Kemenangan';

type OwnProps = {
  onClickEdit?: () => void;
  isDisable?: boolean;
};

type Props = OwnProps & ProkerInterface;

export default Props;
