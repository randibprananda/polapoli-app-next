import { SocialMedinaInterface } from '../../../../@types/Kemenangan';

type Props = {
  form: any;
  onUpdate: (values: any, callback: () => void) => void;
  data?: SocialMedinaInterface;
  isDisable?: boolean;
};

export default Props;
