import CardProfile from '../../../moleculs/KemenanganComponents/CardProfileKemenangan/cardProfileKemenangan.props';

type OwnProps = {
  background?: string;
};

type Props = OwnProps & CardProfile;

export default Props;
