import { DraggerProps } from 'antd/lib/upload';

type Props = {
  draggerProps: DraggerProps;
  loading: boolean;
  form: any;
  isEdit: boolean;
  isDetail?: boolean;
  isPemilih?: boolean;
};

export default Props;
